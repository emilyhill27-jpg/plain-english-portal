# Local-only: Vite picks this up automatically when you run `npm run dev`.
# Tells StateStore to point fetches at the FastAPI backend on :8000.
# This file is NOT used by `npm run build` (production).
VITE_API_BASE=http://127.0.0.1:8000
